1. 读取整个项目，了解项目的整体结构和功能模块。
2. 你需要重构小游戏的前后端项目，项目的核心功能是提交成绩并展示排行榜。项目的代码较为陈旧，存在明显的安全漏洞（如客户端可伪造成绩）。你的任务是重新设计和实现这个项目，使其具备良好的架构、清晰的职责划分，并且能够防止作弊行为。
下面是重构的具体的要求：
"""
## 1. 要交付的东西

可运行的游戏前端+服务端，支持的语言在.tool-versions中指定的版本范围内。接口/协议由你自行设计（请在文档中写清楚）。
- 前端：可以运行小游戏，提交成绩到服务端。
- 后端：重构python2的代码，全部使用python3。提供成绩提交和排行榜查询接口，能防止作弊行为。
- 数据：使用sqlite3存储数据，数据结构自定义。

---

## 2. 你要提供的文档
所有的新增文档都放在 `docs/` 目录下，至少包括以下内容：
1. `README.md`
    - 如何运行前端/后端
    - 如何运行测试
    - 如何提交成绩并查看排行榜
2. `SPEC.md` （也可以拆成 `FRONTEND_SPEC.md`, `BACKEND_SPEC.md`）
    - 你从旧项目（或你的理解）提取/理解/精简的规则与需求
    - 主要边界条件与取舍
3. `ARCHITECTURE.md`
    - 模块划分与职责边界（你认为重要的即可）
    - 关键流程（文字即可）
4. `SECURITY.md`
    - 排行榜在“客户端不可信”前提下的校验
    - 你采用的服务端校验策略：服务端如何判定“是否入榜”
    - 防重放/防刷榜的策略
    - 局限性、薄弱点与应对方法
5. `AI_USAGE.md`（重点）
    - 你如何使用 AI 工具
    - 每一个步骤（至少是每个关键步骤），包括Prompt、验收、修正等

---

## 3. 新增需求

### 3.1 用户识别码

- 每个用户需要有一个唯一的识别码（user_id），可以是UUID或其他形式
- 用户识别码用于区分不同用户的成绩提交和排行榜记录

### 3.2 游戏模式
- 游戏模式：普通单机模式：允许作弊，不计入榜单
- 排行榜模式：防作弊，计入榜单

### 3.3 排行榜

- 设计一个最高分排行榜：按照分数从高到低排序，分数相同则按提交时间先后排序
- 最高分排行榜的分数按照下面的公式计算：score = base_score + time_bonus - miss_penalty + gold_balance_bonus。具体的系数可以微调。
  - base_score: 每个关卡的基础分数
  - time_bonus: 根据游戏结束时间给予的加分，时间越短加分越高。注意time_bonus使用逻辑tick数（endTick / tickRate），与渲染速度无关
  - miss_penalty: 根据漏怪数量扣分，漏怪越多扣分越多
  - gold_balance_bonus: 根据游戏结束时的金币余额给予的加分，余额越高加分越多

### 3.4 排行榜模式的防作弊要求

#### 3.4.1 用户身份识别
- 采用匿名身份机制：首次访问由服务端签发匿名user_id与签名token，客户端本地持久化保存。
- 后续请求使用token认证身份。该机制无需用户名密码即可关联用户历史成绩。
- 对清缓存/换设备导致身份变化的情况，系统视为新匿名用户，并通过限流与风控降低刷榜风险。

### 3.4.2 游戏规则版本
- 维护rulesVersion机制，每次游戏规则或数值参数变更时，升级rulesVersion。
- 客户端提交成绩时，必须提交rulesVersion，服务端拒绝与当前版本不一致的请求
- 所有影响结果的数值参数由服务端维护并版本化。客户端提交回放时仅包含玩家输入，不包含数值参数。服务端按rulesVersion加载官方参数并复算，任何客户端篡改（如修改怪物血量、塔伤害、金币）都会导致复算结果不一致，从而无法通过验证。

#### 3.4.3 前端修改
- 全局禁用Math.random()，使用随机数种子seed机制来确保客户的成绩可以被复现和验证。
- 用tick作为时间，事件只记玩家输入（建塔，升级，卖塔），不记游戏状态（怪物位置，血量等）。
- 引擎加“固定 tick”时钟（逻辑与渲染分离）
- 把所有会占格子、会影响路径/逻辑的东西（比如塔或者墙）都当成实体，并以entityId命名。entityId 由引擎在apply或place时生成并返回。例如下面的Action操作：`{ op: "place", entityType: "tower", entityId: "E12" }`。注意升级不改变entityId，卖掉后entityId失效。
- 对于place操作，entityId由引擎在apply阶段生成并返回，Recorder记录生成后的entityId用于回放；客户端不得预先指定entityId。服务端回放时校验entityId不可复用，upgrade/sell操作只能引用已存在且处于ACTIVE状态的entityId。
- Entity生命周期状态机：
  - NON_EXISTENT：不存在（还没放置 / 已被移除）。填充用。
  - ACTIVE：存在于地图上（可被逻辑使用）。对应的 Action是place和upgrade。
  - REMOVED：已移除（用于审计/回放对账）。对应的 Action是sell。
- 所有“玩家操作入口”统一走一个Action Dispatcher，也就是玩家操作或者replay都走这个入口，必须先通过校验，再执行。
  - 校验包括：
    - 钱够不够（gold >= cost）
    - 位置是否合法（是否在地图内、是否被占用）
    - 升级是否合法（entity_id是否存在）
    - 卖塔或者墙是否合法（entity_id是否存在）
    - 操作顺序是否合理（比如不能在游戏结束后建塔）
    - tick是否单调递增（防replay篡改）
    - 其他
  - 在校验通过后，才真正修改游戏状态，也就是执行：
    - 扣钱
    - 建造并分配entityId
    - 升级
    - 卖出
- 校验失败不记录任何状态改变，直接报错。在校验通过后，才真正修改游戏状态。并由 Recorder记录这条Action。
- Recorder维护（只记录成功执行的操作）：
  - 初始随机种子seed
  - actions事件序列（按tick时间排序，仅记录发生过的操作）
  - 最终的游戏结果（分数，关卡数，金币余额等）
- 按照事件序列record而不是tick序存储，节省空间。例如下面的结构：
```python
actions = [
  { t: 4, op: "place", ... },
  { t: 120, op: "upgrade", ... },
  { t: 300, op: "sell", ... }
]
```
- 同一个tick最多允许2个操作。


#### 3.4.4 后端验证
- 每一局游戏，服务端先发一个只能用一次的编号（attempt_id），并附带随机种子seed，用于防止用户重放。
- 客户端提交成绩时，必须提交attemptId，规则版本号rules_version，声明的分数score_claim, 声明的关卡等级level_claim，以及完整的游戏记录actions。一个attempt_id只能成功一次提交，如果过期，也不能再提交。rules_version与服务端不一致拒绝入榜。
- 服务端根据attempt_id找到对应的seed，使用相同的随机数生成算法和游戏规则，重放actions，计算出最终的分数和关卡数。
- 如果计算出的分数和关卡数与客户端提交的不一致，以服务端为准并入榜。若复算过程中出现非法操作（如金币不足仍建造/升级、entityId 无效、tick 非单调等）则判定无效提交，不入榜。
- 对于同一个用户的重复提交，服务端只接受分数更高的一次入榜。
- 对于短时间内的频繁提交，服务端可以设置冷却时间，防止刷榜行为。
- 只有达到当前Top100 + 20（margin）门槛才进入复算队列。
- 客户端先显示“待验证”，验证通过才计入榜单。
- 不要重复造轮子，调用前端接口replay即可。

---

## 3.5 其他新增的游戏功能
- 设置一个全局的游戏速度控制器，支持玩家以两倍速，四倍速或八倍速玩游戏
- 给防御塔增加攻击音效
- 增加简单的关卡进度系统

---

## 4. 测试
- 前端：使用Jest进行单元测试，覆盖主要逻辑
- 后端：使用pytest进行单元测试，覆盖主要接口和防作弊逻辑

---

## 5. 部署
- 使用docker-compose部署
- 前端和后端分别作为独立的服务运行

---

## 6. 记录AI使用过程（重点）

需要在 `AI_USAGE.md` 中记录过程，目标是让面试官能理解甚至重新操作一遍。

"""

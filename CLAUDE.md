## 重要！！用中文和用户交流

## 项目简介
这是一个基于 HTML5 的塔防小游戏：
- 使用原生 JavaScript + HTML + CSS
- 使用 HTML5 Canvas 渲染

## 项目结构说明
- `src/js/`：核心游戏逻辑
- `src/css/`：样式文件
- `build/td.html`：游戏入口文件
- `screenshots/`：截图
- `tools/`：辅助脚本

## 编码与修改约定（重要）
- 请使用 UTF-8 编码
- 请遵循现有代码风格
- 可以大面积重构代码，但请确保功能完整且正确
- 参考`docs/TODO.md`中的要求生成plan，并记录在`docs/PLAN.md`中

## 工作方式
1. 默认以 `docs/PLAN.md` 为当前任务来源
2. **一次只处理一个任务**
3. 写代码前先说明：
   - 会改哪些文件
   - 为什么要改
   - 改完的预期效果
4. 尽量以 diff / 最小改动形式给出代码
5. 如果需求不清楚，先提问，不要猜

